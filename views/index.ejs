<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
   <%- include('navbar')%>

    <div class="container">
        <br>
        <a class="add" href="/new">Add message</a> <br>
        <div class="messageWrapper">
            <% for(let i = 0; i < messages.length; i++) { %>
                    <a class="btn" href="/messages/<%= i %>">
                    <div class="message">
                        <h2><%= messages[i].user%></h2>
                        <p><%= messages[i].text%></p>
                    </div>
                </a>
            <% } %>
        </div>
    </div>

</body>
</html>